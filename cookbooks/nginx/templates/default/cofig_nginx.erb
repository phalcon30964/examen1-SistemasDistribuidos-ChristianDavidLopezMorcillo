worker_processes  1;
events {
   worker_connections 1024;
}
http {
    upstream webservers {
         server <%=@ipweb1%>;
         server <%=@ipweb2%>;
    }
    server {
        listen 8080;
        location / {
              proxy_pass http://webservers;
        }
    }
}
